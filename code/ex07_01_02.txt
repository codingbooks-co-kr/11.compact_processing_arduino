// 원을 두 개 생성

void setup() {
  size(300, 300);
}

void draw() {
  background(0);
  display(100, 100);		// 함수 호출
  display(200, 200);		// 함수 호출
}

void display(int x, int y) {	// 함수 유형2 (함수 본체)
  fill(255, 255, 0);
  ellipse(x, y, 120, 120);
}