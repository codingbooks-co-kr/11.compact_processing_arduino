// 회전 운동

float theta = 0.0, r = 150.0;

void setup() {
  size(300, 300);
  background(0);
}

void draw() {
  float x = width/2 + r * cos(theta);		// x축 좌표: 코사인(cos) 운동
  float y = height/2 + r * sin(theta);	// y축 좌표: 사인(sin) 운동
  ellipse(x, y, 30, 30);			// 원이 회전 운동
  theta += 0.05;
}