// 문자 세 개 출력

char c1 = 'A', c2 = 'B', c3 = 'C';    // 문자형 변수 선언 및 초기화
int x = 70, y = 70;			// 정수형 변수 선언 및 초기화

size(300, 300);
textSize(50);
text(c1, x, y);				// 문자 출력
text(c2, x*2, y*2);			// 문자 출력
text(c3, x*3, y*3);			// 문자 출력